name: Build Release

on:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "package.json"
      - "webpack.core.config.cjs"
      - "vite.config.js"
      - ".github/workflows/**"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci

      - name: Build All (UserScript, Viewer, GAS Bundle)
        run: |
          npm run build
          # Prepare unified distribution directory for GitHub Pages
          mkdir -p _site
          # Core UserScript
          cp docs/tokiSync.user.js _site/
          # Viewer Application
          cp -r docs/* _site/
          # GAS Bundle
          cp google_app_script/TokiSync_Server_Bundle.gs _site/

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "_site"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
