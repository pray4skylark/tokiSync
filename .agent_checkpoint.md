# TokiSync Session Checkpoint

## 📌 현재 세션 정보
- **상태 업데이트 일시**: 2026-07-20
- **현재 마일스톤**: `v1.27.7` (DI StorageBackend + EventBus + 3-tier branch strategy)
- **브랜치 전략**: `develop` (Beta, -beta.N) → `rc` (RC, -rc.N) → `main` (Stable, vM.m.p)
- **현재 브랜치**: `pray4skylark/develop`

## 🛠️ 수행 완료 내역 (v1.27.7)
### DI StorageBackend + EventBus + Branch Strategy
1. **🏗️ DI 레이어 구축**: `StorageBackend`/`DownloadBackend` 추상 인터페이스 + `GMStorageBackend`/`GMDownloadBackend` 구현체 생성. config.js/queue.js/utils.js/main.js에 DI 주입 패턴 적용
2. **🔌 EventBus TEST_NATIVE_DOWNLOAD**: MenuModal testNativeDownload → EventBus.request(3s timeout) 전환. main.js 리스너 등록
3. **📊 뷰어 deep link 수정**: `useStore.js` 에피소드 파싱 순서 조정
4. **🧪 테스트**: 38/38 Pass (기존 30 + 신규 D1-D8 DI/EventBus)
5. **🏗️ 브랜치 전략 2-tier → 3-tier 전환**: main/develop → main/rc/develop. 태깅 규칙: develop beta.N, rc rc.N, main 정식 태그
6. **🔧 설정 파일 변경**: `build-release.yml` rc 트리거 추가, `vite.config.js` rc basepath, `README.md` RC 링크, `AGENTS.md` 3-Tier Pre-Push, `AI_AGENT_CONTEXT.md` Release 섹션, `CHANGELOG.md` v1.27.7+v1.27.8

## 🛠️ 수행 완료 내역 (v1.27.6)
1. **🐛 `downloader.js:stopAllWorkers()` 경합 방어**: `tokiDownload()` 진입 시 `processingSlots.size > 0 || activeWorkers.size > 0` 감지 → 진행 중인 batch/단일 워커 보호 가드 추가.
2. **📊 `downloader.js:Pre-work progress`**: Drive upload pre-work를 queue 가상 PREP 아이템으로 등록 (0%→5%→30%→50%)
3. **🔑 `queue.js:sessionToken` 복원**: `runSchedulerOnce()`에서 `registerWorkerOrigin` 호출로 정상 nonce 생성
4. **📡 `worker-extractor.js:WORKER_READY sessionToken`**: ts_token → WORKER_READY payload에 sessionToken 포함
5. **빌드 검증**: `npm run build:core` 성공

## 📋 다음 수행 대기 과제 (Next Actions)
### ✨ AGENTS.md 3-Tier 반영 완료
- **상태**: 완료 (Pre-Push 3-tier 분할, Release 체크리스트, 태깅 규칙 변경)
- **Git 실행 남음**: git add → commit → tag v1.27.7-beta.1 → git branch rc

### 📦 RC → Stable 프로모션 (v1.27.7)
- [ ] v1 운동장: 배포 검토 후 git push (사용자)
- [ ] PR: rc → main (사용자)
- [ ] main 병합 후 v1.27.7 정식 태그 (human)
- [ ] GitHub Release 작성, assets 첨부 (human)
