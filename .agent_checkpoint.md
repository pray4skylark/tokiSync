# TokiSync Session Checkpoint

## 📌 현재 세션 정보
- **상태 업데이트 일시**: 2026-09-07
- **현재 마일스톤**: `v1.28.2-rc.4` — 워커 라이프사이클 + 사이트 감지 타이밍 수정 완료 (빌드/테스트 검증 완료, 커밋 대기)
- **브랜치 전략**: `develop` (Beta, -beta.N) → `rc` (RC, -rc.N) → `main` (Stable, vM.m.p)
- **현재 브랜치**: `rc`

## 🛠️ 수행 완료 내역 (v1.28.2-rc.4 — 워커 라이프사이클 + 사이트 감지 수정)

### 배경
- RC2 배포 후 워커 READY 중복 폭주 + 유령 세션 생성 + 사이트 매칭 실패 시 설정 로드 불가 발견
- 계측 빌드(`[Trace]` 로그)로 런타임 타임라인 분석 → 근본 원인 규명

### 수정 6건 (FIX-5~10)
1. **FIX-5 (P1)** `worker-extractor.js` READY 500ms 하트비트 → `load` 이벤트 시 1회 발송
2. **FIX-6 (P1)** `worker-controller.js` 9초 안전 대기 제거 → IPC_ACK + 즉시 START_EXTRACTION
3. **FIX-7 (P1)** `worker-controller.js` 팝업 close 3초 지연 → 즉시 close + nonce 즉시 정리 (RC1 회귀)
4. **FIX-8 (P1)** `downloader.js` READY 30초 타임아웃 (sessionRegistry 기반) — 수집완료 후 타임아웃 지연 발동 방지
5. **FIX-9 (P2)** `MenuModal.js` getInstance() 핸들러 없이 생성 방지 → 설정 로드 정상화
6. **FIX-10 (P2)** `main.js` + `MenuModal.js` + `EventBus.js` 사이트 감지 타이밍 변경 + 재시도 버튼 UI

### 조사 결과
- GenericParser 변경은 토큰 타임아웃 원인이 **아님** (A/B 테스트로 확인)
- 토큰 타임아웃은 GAS 서버 일시적 지연 (코드 문제 아님)
- 워커 READY 폭주의 근본 원인: `setInterval(500ms)` 하트비트 + `removeWorkerOrigin` 3초 지연

### 테스트
- RC1 GenericParser vs RC2 GenericParser A/B 테스트
- 계측 빌드 런타임 분석
- 웹툰/소설 배치 다운로드 테스트

## 📋 다음 수행 대기 과제 (Next Actions)

### 🏷️ RC 커밋/태그 (v1.28.2-rc.4)
- [x] 버전 범프: package.json → components.script/viewer/gas (`v1.28.2-rc.4`)
- [x] 빌드: `npm run build:core` 성공
- [x] 문서화: CHANGELOG.md + .agent_checkpoint.md 업데이트
- [ ] 커밋: `git commit`
- [ ] 태그: `v1.28.2-rc.4`
- [ ] Push: `git push origin rc --tags` (사용자 직접 실행)

## 🛠️ 수행 완료 내역 (v1.28.2-rc.2 — 에지케이스 검증 기반 수정)

### 배경
- RC 브랜치 전수 에지케이스 분석(27건) → 실코드 대조 검증 → 원 보고 P1 2건 모두 과장 판정
- 검증 중 신규 발견: 진짜 P1 1건(getRawQueue ReferenceError) + P2 회귀 1건(_activeProcessing 소거)

### 수정 4건 (FIX-1~4)
1. **FIX-1 (P1)** `downloader.js` processItem finally가 미수출 함수 참조 → ReferenceError로 현재 에피소드 다운로드(만화/비합본) 저장 실패. `getQueue()`로 교체
2. **FIX-2 (P2)** `worker-controller.js` handleBatchSuccess의 `_activeProcessing.add`가 destroyWorkerSession(v1.28.1부터 delete 포함)에 즉시 소거 → 업로드 중 병렬 수집 시작(직렬 위반). 순서 스왑으로 가드 복원
3. **FIX-3 (P2)** `GenericParser.js` imageContainer 미매칭 fallback이 exclude/remove 규칙·동적 키 탐지 스킵하며 광고 유입 → early-return 제거 후 정상 흐름 합류
4. **FIX-4 (P2)** `QUEUE_STOP_ALL`/`QUEUE_RESET` 이벤트 미방출(dead listener) + masterZipCache 중단 미정리 → queue.js emit 배선 + `flushMasterZipCache()` 헬퍼 추출. 수집 중단 시 부분 ZIP 저장 후 폐기(완료분 보존 + 재다운로드 혼입 차단), 전체 초기화 시 증발 폐기

### 테스트
- E14(업로드 페이즈 세션 생존 계약), E15(캐시 정리 이벤트 방출) 런타임 추가
- M33/M34 static-verify 규칙 추가 (미수출 함수 참조 금지 / destroy 선행 순서)
- ⚠️ 체크포인트 구안의 DOM Fixture 테스트 예약 번호 E14-E16은 이미 사용됨 → fixture 구현 시 E16부터 사용

### 검증 결과
- `npm run test`: Unit 52 + Real-env 3 + Static 10 전부 통과 ✅
- `npm run build:core`: 성공 (620 KiB bundle, CSS 감사 통과) ✅

### 의도적으로 범위 제외 (다음 develop 사이클 권장)
- EC-1/EC-2: closeActiveWorker/processItem 수동 registry 삭제 → destroyWorkerSession 통일 (P3 코드냄새)
- EC-9: getQueuePaused/setQueuePaused DI 전환 (P3, 크로스탭 pause 경로라 회귀 테스트 필요)

## 📋 다음 수행 대기 과제 (Next Actions)

### 🏷️ RC 커밋/태그 (v1.28.2-rc.2)
- [x] 버전 범프: package.json → components.script/viewer/gas (`v1.28.2-rc.2`)
- [x] Git: 커밋 2건 분리 (fix 4건 / feat forceClose SOS) + 문서화 커밋
- [ ] 태그: `v1.28.2-rc.2` (문서화 커밋에 부여 예정)
- [ ] Push: `git push origin rc --tags` (사용자 직접 실행)

## 🛠️ 수행 완료 내역 (v1.28.1)

### 4-Phase 안정성 강화
1. **Phase 1: Series Config DI 통합**: `series-config.js`에 StorageBackend DI 적용, 이중 직렬화 제거, v1.27.x 문자열 호환 폴백. `main.js`에 `setSeriesStorage()` 주입
2. **Phase 2: Abort/Error 경로 정리**: `saveSeriesConfig` 실패 시 inline fallback (P1), `runDrivePreWork` try/finally로 prepItem 항상 정리, `tokiDownload` abort 경로 queue 정리, addEpisodesToQueue 중복 호출 제거, PrepItem ID random suffix, getSeriesConfigKey 해시 충돌 방지
3. **Phase 3: Session Consistency**: `assertConsistent` 멤버십 검사 + 3-way 자가 치유 (processingSlots/sessionRegistry/activeWorkers), export 추가. `deleteSeriesConfig` TOCTOU 방지 (sameSeriesActive 검사). `processItem` queue item 충돌 방지
4. **Phase 4: UX/Visibility**: `STORAGE_FATAL` UI 리스너 (LogBox toast+alert), `EventBus.request()` pre-emit listener check, `config.js` one-time console.debug

### 크로스검증 + 에지케이스
- solver-a(방어/보안) + solver-b(UX/운영) + cross-validator로 4건 보완사항 검증
- 11건 에지케이스 식별 → P1 1건, P2 8건, P3 5건 모두 수정 완료
- 수렴 결론: `STORAGE_FATAL` UI 리스너(P2), `assertConsistent` 자가 치유(P2) 우선 적용. MV3→localStorage 전환은 INVALID

### 테스트 통합
- E1-E13 에지케이스 회귀 신규 추가 (50/50 Unit)
- mockStorage() 헬퍼 추출 (9중복→1함수, ~40줄 절감)
- M2/E9 통합, assertConsistent 직접 검증 (4시나리오)
- 실환경 복구: `window.addEventListener` JSDOM mock (0/3→3/3)
- **검증**: `npm run build:core` ✅, `npm run test` **61/61 Pass** (50 Unit + 3 Real + 8 Static)

### AGENTS.md 규칙화
- DI Storage Mandate + EventBus Patterns + Queue State Consistency + Storage Safety Rules + Queue Key Splitting + Test Conventions (6개 신규 섹션, 153→215줄)

### 문서화 완료
- `CHANGELOG.md` v1.28.1 엔트리 추가
- `documentation/reports/RELEASE_NOTE_v1.28.0.md` 통합 릴리즈 노트 작성
- `AGENTS.md` 6개 신규 규칙 섹션
- `.agent_checkpoint.md` 갱신

## 🛠️ 수행 완료 내역 (v1.27.6)
1. **🐛 `downloader.js:stopAllWorkers()` 경합 방어**: `tokiDownload()` 진입 시 `processingSlots.size > 0 || activeWorkers.size > 0` 감지 → 진행 중인 batch/단일 워커 보호 가드 추가.
2. **📊 `downloader.js:Pre-work progress`**: Drive upload pre-work를 queue 가상 PREP 아이템으로 등록 (0%→5%→30%→50%)
3. **🔑 `queue.js:sessionToken` 복원**: `runSchedulerOnce()`에서 `registerWorkerOrigin` 호출로 정상 nonce 생성
4. **📡 `worker-extractor.js:WORKER_READY sessionToken`**: ts_token → WORKER_READY payload에 sessionToken 포함
5. **빌드 검증**: `npm run build:core` 성공

## 📋 다음 수행 대기 과제 (Next Actions)

### 🏷️ RC 승격 (v1.28.1-rc.1)
- [x] 문서화 완료 (CHANGELOG, RELEASE_NOTE, .agent_checkpoint)
- [x] 빌드 검증: `npm run build:core` ✅
- [x] 테스트 검증: 61/61 Pass ✅
- [x] Graphify 갱신
- [ ] Git: commit → tag `v1.28.1-rc.1`
- [ ] Push: `git push origin develop --tags` + `git push origin rc:rc` (사용자)

### 🧪 DOM Fixture 기반 Mock Test (향후 구현)
- **목적**: 실제 사이트 HTML 스냅샷을 `test/fixtures/`에 저장 → JSDOM에서 파서 정확도 회귀 테스트
- **6단계**: Fixture 수집 도구 → test-fixture.js → fixtures 저장소 → E14-E16 → CI 통합 → npm script
- **파일**: `test/fixtures/manatoki_001.json` 등의 포맷으로 관리

### 📦 RC → Stable 프로모션
- [ ] PR: `rc` → `main` (사용자)
- [ ] main 병합 후 v1.28.1 정식 태그 (human)
- [ ] GitHub Release 작성, assets 첨부 (human)
