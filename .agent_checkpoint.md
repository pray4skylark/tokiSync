# TokiSync Session Checkpoint

## 📌 현재 세션 정보
- **상태 업데이트 일시**: 2026-07-22
- **현재 마일스톤**: `v1.28.1` — RC 승격 가능 (61/61 테스트 통과, 문서화 완료)
- **브랜치 전략**: `develop` (Beta, -beta.N) → `rc` (RC, -rc.N) → `main` (Stable, vM.m.p)
- **현재 브랜치**: `pray4skylark/develop`

## 🛠️ 수행 완료 내역 (v1.28.1)

### 4-Phase 안정성 강화
1. **Phase 1: Series Config DI 통합**: `series-config.js`에 StorageBackend DI 적용, 이중 직렬화 제거, v1.27.x 문자열 호환 폴백. `main.js`에 `setSeriesStorage()` 주입
2. **Phase 2: Abort/Error 경로 정리**: `saveSeriesConfig` 실패 시 inline fallback (P1), `runDrivePreWork` try/finally로 prepItem 항상 정리, `tokiDownload` abort 경로 queue 정리, addEpisodesToQueue 중복 호출 제거, PrepItem ID random suffix, getSeriesConfigKey 해시 충돌 방지
3. **Phase 3: Session Consistency**: `assertConsistent` 멤버십 검사 + 3-way 자가 치유 (processingSlots/sessionRegistry/activeWorkers), export 추가. `deleteSeriesConfig` TOCTOU 방지 (sameSeriesActive 검사). `processItem` queue item 충돌 방지
4. **Phase 4: UX/Visibility**: `STORAGE_FATAL` UI 리스너 (LogBox toast+alert), `EventBus.request()` pre-emit listener check, `config.js` one-time console.debug

### 크로스검증 + 에지케이스
- solver-a(방어/보안) + solver-b(UX/운영) + cross-validator로 4건 보완사항 검증
- 11건 에지케이스 식별 → P1 1건, P2 8건, P3 5건 모두 수정 완료
- 수렴 결론: `STORAGE_FATAL` UI 리스너(P2), `assertConsistent` 자가 치유(P2) 우선 적용. MV3→localStorage 전환은 INVALID

### 테스트 통합
- E1-E13 에지케이스 회귀 신규 추가 (50/50 Unit)
- mockStorage() 헬퍼 추출 (9중복→1함수, ~40줄 절감)
- M2/E9 통합, assertConsistent 직접 검증 (4시나리오)
- 실환경 복구: `window.addEventListener` JSDOM mock (0/3→3/3)
- **검증**: `npm run build:core` ✅, `npm run test` **61/61 Pass** (50 Unit + 3 Real + 8 Static)

### AGENTS.md 규칙화
- DI Storage Mandate + EventBus Patterns + Queue State Consistency + Storage Safety Rules + Queue Key Splitting + Test Conventions (6개 신규 섹션, 153→215줄)

### 문서화 완료
- `CHANGELOG.md` v1.28.1 엔트리 추가
- `documentation/reports/RELEASE_NOTE_v1.28.0.md` 통합 릴리즈 노트 작성
- `AGENTS.md` 6개 신규 규칙 섹션
- `.agent_checkpoint.md` 갱신

## 🛠️ 수행 완료 내역 (v1.27.6)
1. **🐛 `downloader.js:stopAllWorkers()` 경합 방어**: `tokiDownload()` 진입 시 `processingSlots.size > 0 || activeWorkers.size > 0` 감지 → 진행 중인 batch/단일 워커 보호 가드 추가.
2. **📊 `downloader.js:Pre-work progress`**: Drive upload pre-work를 queue 가상 PREP 아이템으로 등록 (0%→5%→30%→50%)
3. **🔑 `queue.js:sessionToken` 복원**: `runSchedulerOnce()`에서 `registerWorkerOrigin` 호출로 정상 nonce 생성
4. **📡 `worker-extractor.js:WORKER_READY sessionToken`**: ts_token → WORKER_READY payload에 sessionToken 포함
5. **빌드 검증**: `npm run build:core` 성공

## 📋 다음 수행 대기 과제 (Next Actions)

### 🏷️ RC 승격 (v1.28.1-rc.1)
- [x] 문서화 완료 (CHANGELOG, RELEASE_NOTE, .agent_checkpoint)
- [x] 빌드 검증: `npm run build:core` ✅
- [x] 테스트 검증: 61/61 Pass ✅
- [x] Graphify 갱신
- [ ] Git: commit → tag `v1.28.1-rc.1`
- [ ] Push: `git push origin develop --tags` + `git push origin rc:rc` (사용자)

### 🧪 DOM Fixture 기반 Mock Test (향후 구현)
- **목적**: 실제 사이트 HTML 스냅샷을 `test/fixtures/`에 저장 → JSDOM에서 파서 정확도 회귀 테스트
- **6단계**: Fixture 수집 도구 → test-fixture.js → fixtures 저장소 → E14-E16 → CI 통합 → npm script
- **파일**: `test/fixtures/manatoki_001.json` 등의 포맷으로 관리

### 📦 RC → Stable 프로모션
- [ ] PR: `rc` → `main` (사용자)
- [ ] main 병합 후 v1.28.1 정식 태그 (human)
- [ ] GitHub Release 작성, assets 첨부 (human)
